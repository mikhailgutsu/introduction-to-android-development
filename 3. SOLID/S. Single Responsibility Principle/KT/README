Что здесь не так?
UserManager делает слишком много: управляет пользователями, взаимодействует с базой данных и логирует действия.
Если изменится структура базы данных, придется менять этот класс.
Если изменится способ логирования, опять придется менять этот класс.
Нарушен SRP: у класса несколько причин для изменения.

Что улучшилось?
✅ UserService отвечает только за управление пользователями.
✅ UserRepository изолирован, и любые изменения в БД не затронут UserService.
✅ Logger отдельный, и его можно легко заменить (например, на логирование в файл).
✅ Код стал гибче и легче тестировать.

Таким образом, применяя принцип SRP, мы разделяем ответственность и делаем код более гибким, масштабируемым и тестируемым. 🚀